import { IsQuotePostPipe } from './is-quote-post.pipe';

describe('IsQuotePostPipe', () => {
  it('create an instance', () => {
    const pipe = new IsQuotePostPipe();
    expect(pipe).toBeTruthy();
  });
});
