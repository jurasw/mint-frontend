import { ValidationInfoPipe } from './validation-info.pipe';

describe('ValidationInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidationInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
